#!/usr/bin/env escript

main(Args) ->
    Script = filename:absname(escript:script_name()),
    Root = filename:dirname(filename:dirname(Script)),
    _ = code:add_patha(filename:join(Root, "ebin")),
    pyrunicorn_cli:main(Args).
